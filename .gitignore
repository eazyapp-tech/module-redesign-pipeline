# Operator-private, never published. Both are built from a specific named
# stakeholder's own words on real projects, so they carry that person's
# corrections, register and business numbers.
#
# The line this repo holds: the generalisable method is public; anything
# derived from one stakeholder's own words is private. Both live under
# ~/.claude/docs/working-records/ and are backed up in a private repo.
references/round-protocol.md
lessons.md
working-records/
